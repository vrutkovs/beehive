issue.features:
===============================================================================

:Status:   PREPARED (fixes are being applied).
:Requires: Python >= 2.6 (due to step implementations)

This directory contains beehive self-tests to ensure that beehive related
issues are fixed.

PROCEDURE:

    * ONCE: Install python requirements ("requirements.txt")
    * Run the tests with beehive

::

    bin/beehive -f progress issue.features/
